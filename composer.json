{
    "name": "jakubboucek/esc-pos",
    "description": "Native ESC-POS drivers",
    "type": "library",
    "license": "MIT",
    "authors": [
        {
            "name": "Jakub Bouček",
            "email": "pan@jakubboucek.cz"
        }
    ],
    "minimum-stability": "dev",
    "require": {
        "ext-sockets": "*",
        "ext-iconv": "*"
    },
    "autoload": {
        "psr-4": {
            "JakubBoucek\\EscPos\\": "src/"
        }
    },
    "scripts": {
        "phpstan": "docker run --rm -v `pwd`:/www phpstan/phpstan analyse -c /www/phpstan.neon --level 5"
    }
}
